"""odrkit — the deterministic reporting engine behind Ops Data Reporter (ODR).

Same role + period + format always produces the same report, computed by
tested code, never by an LLM. See the repo README.md and CLAUDE.md.
"""

__version__ = "0.1.0"
